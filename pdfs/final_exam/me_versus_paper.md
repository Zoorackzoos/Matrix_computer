Recall: O is an orthgonal matrix if fs col^s are orthgonal <---> O^-1 = O^T <---> O^T is orthgonal
Also: if V \e R^n ; O is orthgonal
Then:
||O*v|| = || v ||
||O*v||^2   = (O * v) * (O * v)
            = (O * v)^T * (O v)
            = V^T * O^T * O * V = v^T * v = v * v
                                            = || v ||^2
wtf am i dong? 

the actual fucking paper instead of this yapping nonsnese

problem 1. find all eigenvalues and compute their algebraic and geometric multiplies
2   1   0   0
0   2   0   0
0   0   3   1
0   0   0   3

\l = 2, 3
    it's just where the diagonals = 0 
    right? 

problem 2 for each part below, either give us example of a square matrix A with the requested properties or else explain why this is impossible
(A) invertible but not diagonalized
    0   1   2
    0   0   1
    0   0   0
        if Det(A) = 0 then not invertible 
    
(b) diagonalize but not invertible
(C) neither invertible nor diagonalizing
(d) both invertible and diagonalize 

