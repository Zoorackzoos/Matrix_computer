# question 1
for each of these circle "true" or "false" to indicate whether it is true or false. No justification is lrequired

if A and B are row-equivalent matrices then we always have col(A) = col(B)
    true
if A and B are row-equivalent matrices then we always have row(A) = Row(B0
    true
for any matrix A, dim(row(A)) = dim(col(A))
    false
A system omf 3 equations is 2 unknowns must be inconsistent


